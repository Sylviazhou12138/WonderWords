# WonderWords - 版本历史

## v1.0 - 2025-12-07

**首个完整工作版本** 🎉

### 功能特性
- ✅ 自动在 YouTube 视频页面注入 "✨ Analyze Words" 按钮
- ✅ 从 YouTube 视频获取英文字幕（使用 `youtube-transcript-api`）
- ✅ 通过 Google Gemini AI 分析提取 B2+ 难度的高级词汇和习语
- ✅ 在右侧边栏以精美卡片形式显示：
  - 单词/短语
  - 中文释义
  - 原句上下文

### 技术架构
```
Chrome Extension (content.js)
    ↓ 获取 video-id
    ↓ HTTP Request
Flask Server (transcript_server.py, port 5001)
    ↓ subprocess call
Python Script (get_transcript.py)
    ↓ youtube-transcript-api
YouTube API → 返回字幕
    ↓
Chrome Extension
    ↓ 调用 Gemini API
Google Gemini AI
    ↓ 返回单词分析
侧边栏显示结果
```

### 依赖版本
- `youtube-transcript-api`: 1.2.3+
- `flask`: 3.0.0
- `flask-cors`: 4.0.0
- Google Gemini API: v1beta

### 使用的 Gemini 模型
- `gemini-flash-latest` (主要)
- `gemini-2.5-flash` (备用)
- `gemini-2.0-flash-exp` (备用)

### 文件结构
```
WonderWords/
├── manifest.json          # Chrome 扩展配置
├── content.js             # 主要逻辑（v4.0）
├── background.js          # 后台服务
├── popup.html/js          # 设置页面
├── transcript_server.py   # Flask API 服务器（v4.0）
├── get_transcript.py      # 独立字幕获取脚本
├── requirements.txt       # Python 依赖
├── setup.sh              # 一键安装脚本
├── start_server.sh       # 启动服务器脚本
└── README.md             # 项目说明
```

### 已知限制
1. 需要手动启动 Python 服务器（`./start_server.sh`）
2. 仅支持有英文字幕的视频
3. Gemini API 有免费配额限制（每分钟 60 次请求）

### 开发历程
- 尝试了多种字幕获取方案（DOM 提取、直接 API 调用）
- 最终通过独立 Python 脚本 + subprocess 调用成功绕过 YouTube 限制
- 解决了 Gemini API 模型名称问题（v1beta + 正确的模型名）

---

## v1.1 - 2025-12-11

**用户自定义设置功能** ⚙️

### 新增功能

#### 1. 难度级别选择
用户可以自主选择词汇难度级别（基于 CEFR 标准）：
- **A1** - Beginner (基础词汇)
- **A2** - Elementary (初级词汇)
- **B1** - Intermediate (中级词汇)
- **B2** - Upper Intermediate (中高级词汇) ← 默认
- **C1** - Advanced (高级词汇)
- **C2** - Proficiency (精通级词汇)

**实现细节：**
- AI prompt 动态调整，要求提取指定 CEFR 级别的词汇
- 设置通过 `chrome.storage.sync` 持久化保存
- 跨设备同步（通过 Google 账号）

#### 2. 母语选择
用户可以选择词汇定义的目标语言：
- 中文 (Chinese) ← 默认
- 日本語 (Japanese)
- 한국어 (Korean)
- Español (Spanish)
- Français (French)
- Deutsch (German)
- English (English)

**实现细节：**
- AI prompt 中指定目标语言
- 所有词汇定义将以用户选择的语言返回

#### 3. 改进的设置界面
- **设置按钮**：侧边栏右上角的 ⚙️ 图标
- **设置面板**：
  - 难度级别下拉菜单
  - 母语选择下拉菜单
  - 保存按钮（立即生效）
  - 返回按钮（查看结果）
- **状态显示**：侧边栏顶部显示当前设置
  ```
  Level: B2 | Language: 中文
  ```

### UI/UX 改进
- 侧边栏宽度增加到 350px（原 300px）
- 词汇卡片增加蓝色左边框（视觉层次）
- 改进的间距和排版
- 关闭按钮使用红色（更明显）
- 设置和内容的流畅切换动画

### 技术更新

**content.js 更新：**
```javascript
// 新增默认设置
const DEFAULT_SETTINGS = {
  difficulty: "B2",
  nativeLanguage: "Chinese",
};

// 新增语言映射
const LANGUAGE_MAP = { Chinese: "中文", Japanese: "日本語", ... };

// 新增异步设置管理
async function getSettings() { ... }
async function saveSettings(settings) { ... }

// AI prompt 动态生成
const prompt = `Extract ${difficulty} level words... 
Definitions in ${targetLanguage}...`;
```

**manifest.json 更新：**
```json
{
  "version": "1.1",
  "permissions": ["activeTab", "scripting", "storage"]  // ← 已包含 storage
}
```

### 使用示例

**场景 1：初学者模式**
1. 打开设置
2. 选择难度 "A1 - Beginner"
3. 选择母语 "日本語"
4. 保存设置
5. 重新分析视频 → 获得日语解释的基础词汇

**场景 2：高级学习者**
1. 选择难度 "C2 - Proficiency"
2. 选择母语 "English"（用英语解释英语）
3. 分析学术视频 → 获得精通级词汇

### 设置持久化
- 使用 `chrome.storage.sync` API
- 设置会在所有登录同一 Google 账号的 Chrome 浏览器间同步
- 关闭浏览器后设置依然保留

### 测试说明
详见 `TESTING_v1.1.md`

### 下一步计划
- [ ] 自动启动 Python 服务器
- [ ] 支持多语言字幕（不仅限英文）
- [ ] 添加单词收藏功能
- [ ] 导出单词列表（Anki、CSV 等格式）
- [ ] 改进 AI 分析准确性（更好的 prompt engineering）
- [ ] 添加词汇数量自定义（当前固定 5-8 个）
